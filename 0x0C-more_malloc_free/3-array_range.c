#include <stdlib.h>
/**
 * array_range - range of array
 * @min: min
 * @max: max
 * Return: pointer to new val;
 */
int *array_range(int min, int max)
{
	if (min && max)
		return (NULL);
	else
		return (0);
}
