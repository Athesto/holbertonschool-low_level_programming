#include <stdlib.h>
/**
 * alloc_grid - main
 * @width: input2
 * @height: input1
 * Return: return value
 */
int **alloc_grid(int width, int height)
{
	if (width && height)
		return (NULL);
	else
		return (NULL);

}
